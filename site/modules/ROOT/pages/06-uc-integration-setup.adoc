= Deep Dive: Setting Up the Neo4j Federated JDBC UC Connection
:description: Step-by-step walkthrough of connecting Neo4j to Databricks Unity Catalog via JDBC, including the SafeSpark fix, SQL-to-Cypher translation, and validated query patterns

[.lead]
The federated queries in this guide depend on a Neo4j Federated JDBC UC Connection. Getting that connection working required solving a SafeSpark sandbox memory issue in collaboration with Databricks engineering. This section walks through the full setup, from uploading JARs to running validated queries, so the integration can be reproduced in any Databricks workspace.

== The Problem

Databricks Unity Catalog supports JDBC connections to external databases through https://docs.databricks.com/query-federation/[Lakehouse Federation]. In theory, any database with a JDBC driver can participate. In practice, connecting Neo4j hit a wall.

The Neo4j Python driver worked. The Neo4j Spark Connector worked. Direct JDBC outside of Unity Catalog worked. The Neo4j Federated JDBC UC Connection failed every time with the same error:

[source,text]
----
Connection was closed before the operation completed
----

The error originated in `com.databricks.safespark.jdbc.grpc_client.JdbcConnectClient.awaitWhileConnected`, which pointed to the SafeSpark sandbox, not Neo4j itself. SafeSpark is the isolation layer Databricks uses to run JDBC drivers in a sandboxed JVM, preventing untrusted driver code from accessing cluster internals. The Neo4j JDBC driver, with its built-in SQL-to-Cypher translator, loads more classes during initialization than most JDBC drivers. The default SafeSpark sandbox allocated too little metaspace memory for that class loading, so the sandboxed JVM crashed silently.

== The Fix: SafeSpark Memory Configuration

Three Spark configuration properties control the SafeSpark sandbox memory allocation. Adding these to the Databricks cluster configuration resolves the issue:

[source,properties]
----
spark.databricks.safespark.jdbcSandbox.jvm.maxMetaspace.mib 128
spark.databricks.safespark.jdbcSandbox.jvm.xmx.mib 300
spark.databricks.safespark.jdbcSandbox.size.default.mib 512
----

These go in the cluster's **Advanced Options > Spark > Spark Config** section. Without them, every Neo4j Federated JDBC UC Connection query to Neo4j will fail with the "Connection was closed" error, regardless of whether the query itself is valid.

== Prerequisites

Before creating the connection, three things need to be in place.

=== Databricks Preview Features

Two preview features must be enabled in the Databricks workspace:

[cols="1,2"]
|===
|Feature |Purpose

|Custom JDBC on UC Compute
|Allows loading custom JDBC driver JARs in UC connections

|`remote_query` table-valued function
|Enables the `remote_query()` SQL function for querying external sources
|===

=== JDBC Driver JARs

The Neo4j JDBC driver ships as two JARs that need to be uploaded to a Unity Catalog Volume:

[cols="1,2"]
|===
|JAR |Purpose

|`neo4j-jdbc-full-bundle-6.10.3.jar`
|The JDBC driver itself, including the SQL-to-Cypher translation engine

|`neo4j-jdbc-translator-sparkcleaner-6.10.3.jar`
|Cleans Spark-generated SQL artifacts before translation (removes `SPARK_GEN_SUBQ` wrappers and similar constructs)
|===

Upload both to a UC Volume path, for example `/Volumes/catalog/schema/jars/`. The `CREATE CONNECTION` statement references these paths directly, so the Volume must be accessible from the cluster.

=== Credentials in Databricks Secrets

Store Neo4j credentials in a Databricks secret scope rather than hardcoding them. The test suite uses a scope called `neo4j-uc-creds` with these keys:

[source,python]
----
SCOPE_NAME = "neo4j-uc-creds"

NEO4J_HOST = dbutils.secrets.get(SCOPE_NAME, "host")
NEO4J_USER = dbutils.secrets.get(SCOPE_NAME, "user")
NEO4J_PASSWORD = dbutils.secrets.get(SCOPE_NAME, "password")
JDBC_JAR_PATH = dbutils.secrets.get(SCOPE_NAME, "jdbc_jar_path")
CLEANER_JAR_PATH = dbutils.secrets.get(SCOPE_NAME, "cleaner_jar_path")
UC_CONNECTION_NAME = dbutils.secrets.get(SCOPE_NAME, "connection_name")
----

A `setup.sh` script in the test suite reads from a `.env` file and configures these secrets automatically using the Databricks CLI.

== Creating the Neo4j Federated JDBC UC Connection

With the prerequisites in place, the connection is a single SQL statement:

[source,sql]
----
CREATE CONNECTION neo4j_connection TYPE JDBC
ENVIRONMENT (
  java_dependencies '[
    "/Volumes/catalog/schema/jars/neo4j-jdbc-full-bundle-6.10.3.jar",
    "/Volumes/catalog/schema/jars/neo4j-jdbc-translator-sparkcleaner-6.10.3.jar"
  ]'
)
OPTIONS (
  url 'jdbc:neo4j+s://your-host:7687/neo4j?enableSQLTranslation=true',
  user secret('neo4j-uc-creds', 'user'),
  password secret('neo4j-uc-creds', 'password'),
  driver 'org.neo4j.jdbc.Neo4jDriver',
  externalOptionsAllowList 'dbtable,query,customSchema'
)
----

A few things to note about this statement.

The `ENVIRONMENT` block uses `java_dependencies` to load the two JARs from the UC Volume. This is how the SafeSpark sandbox gets the driver classes. Both JARs are required. The spark cleaner JAR preprocesses Spark-generated SQL before the main driver translates it to Cypher.

NOTE: `java_dependencies` only accepts UC Volume paths. Cluster-installed libraries (e.g., Maven coordinates added via the cluster Libraries tab) cannot be referenced here — they are a separate system.

The JDBC URL includes `enableSQLTranslation=true` as a query parameter. This activates the driver's built-in SQL-to-Cypher translator, which is what makes the whole federation approach work. Without it, the driver expects native Cypher and Spark's SQL queries would fail.

The `externalOptionsAllowList` property is significant. By default, Neo4j Federated JDBC UC Connections only allow connection-level options. Adding `customSchema` to the allow list lets individual queries specify their own schema, which turns out to be mandatory for Neo4j (explained in the next section).

To verify the connection was created correctly:

[source,sql]
----
DESCRIBE CONNECTION neo4j_connection
----

== How SQL-to-Cypher Translation Works

The Neo4j JDBC driver translates standard SQL into Cypher automatically. Neo4j labels become SQL tables, relationship types become JOIN targets, and node properties become columns. This translation is what allows Databricks to treat Neo4j as a SQL-compatible data source.

Here are the translations validated against the test suite:

[cols="1,1"]
|===
|SQL |Cypher

|`SELECT COUNT(*) FROM Flight`
|`MATCH (n:Flight) RETURN count(n)`

|`SELECT COUNT(*) FROM Aircraft WHERE manufacturer = 'Boeing'`
|`MATCH (n:Aircraft) WHERE n.manufacturer = 'Boeing' RETURN count(n)`

|`SELECT COUNT(DISTINCT manufacturer) FROM Aircraft`
|`MATCH (n:Aircraft) RETURN count(DISTINCT n.manufacturer)`

|`FROM Flight f NATURAL JOIN DEPARTS_FROM r NATURAL JOIN Airport a`
|`MATCH (f:Flight)-[:DEPARTS_FROM]->(a:Airport)`
|===

The `NATURAL JOIN` syntax maps to Cypher relationship traversals. The first table is the source node label, the middle "table" is the relationship type, and the last table is the target node label. This is the mechanism behind the graph traversal counts in the federated queries.

== The customSchema Requirement

Spark's JDBC reader runs a schema inference step before executing any query. It sends a modified version of the query with `WHERE 1=0` to get column metadata without fetching actual data. Neo4j's JDBC driver returns `NullType()` for all columns during this inference step, which causes Spark to fail with a `No column has been read prior to this call` error.

The fix is to always provide an explicit `customSchema` option that tells Spark the column names and types, bypassing inference entirely:

[source,python]
----
# This fails - Spark can't infer the schema from Neo4j
df = spark.read.format("jdbc") \
    .option("databricks.connection", "neo4j_connection") \
    .option("query", "SELECT COUNT(*) AS cnt FROM Flight") \
    .load()

# This works - schema is specified explicitly
df = spark.read.format("jdbc") \
    .option("databricks.connection", "neo4j_connection") \
    .option("query", "SELECT COUNT(*) AS cnt FROM Flight") \
    .option("customSchema", "cnt LONG") \
    .load()
----

Every query through the Neo4j Federated JDBC UC Connection needs `customSchema`. There are no exceptions. The column names in the schema must match the aliases in the SQL query, and the types must match what Neo4j returns (typically `LONG` for counts, `STRING` for text properties, `DOUBLE` for floating-point values).

== Two Ways to Query

Once the connection exists, there are two ways to use it.

=== Spark DataFrame API

The DataFrame API uses `spark.read.format("jdbc")` with the `databricks.connection` option pointing to the UC connection name:

[source,python]
----
df = spark.read.format("jdbc") \
    .option("databricks.connection", "neo4j_connection") \
    .option("query", "SELECT COUNT(*) AS cnt FROM Flight") \
    .option("customSchema", "cnt LONG") \
    .load()

df.show()
# +----+
# | cnt|
# +----+
# | 800|
# +----+
----

This returns a standard Spark DataFrame that can be joined with Delta tables, registered as a temp view, or used in any Spark operation.

=== remote_query() SQL Function

The `remote_query()` function lets SQL queries reference the connection directly, without PySpark:

[source,sql]
----
SELECT * FROM remote_query(
    'neo4j_connection',
    query => 'SELECT COUNT(*) AS cnt FROM Flight'
)
----

This is pure SQL, which makes it useful for dashboards, SQL notebooks, and cases where the query result can be used directly without further DataFrame transformations. It also handles schema inference internally, so `customSchema` is not required.

== What Works and What Doesn't

Spark wraps JDBC queries in subqueries for schema resolution. The wrapping looks like `SELECT * FROM (your_query) SPARK_GEN_SUBQ_0 WHERE 1=0`. The spark cleaner JAR strips most of these artifacts, but certain SQL constructs still do not translate.

=== Supported Patterns

These SQL patterns have been validated through Neo4j Federated JDBC UC Connection with 100% pass rate:

[cols="2,1,2"]
|===
|Pattern |Status |Example

|Simple expressions
|PASS
|`SELECT 1 AS test`

|COUNT aggregate
|PASS
|`SELECT COUNT(*) AS cnt FROM Flight`

|Multiple aggregates
|PASS
|`SELECT COUNT(*), MIN(aircraft_id), MAX(aircraft_id) FROM Aircraft`

|COUNT DISTINCT
|PASS
|`SELECT COUNT(DISTINCT manufacturer) FROM Aircraft`

|Aggregate with WHERE (equals)
|PASS
|`WHERE manufacturer = 'Boeing'`

|Aggregate with WHERE (IN)
|PASS
|`WHERE manufacturer IN ('Boeing', 'Airbus')`

|Aggregate with WHERE (AND)
|PASS
|`WHERE manufacturer = 'Boeing' AND model IS NOT NULL`

|Aggregate with WHERE (IS NOT NULL)
|PASS
|`WHERE icao24 IS NOT NULL`

|JOIN with aggregate
|PASS
|`FROM Flight NATURAL JOIN DEPARTS_FROM NATURAL JOIN Airport`
|===

=== New SQL Functionality Supported

The SQL-to-Cypher translator supports GROUP BY, HAVING, ORDER BY, LIMIT/OFFSET, DISTINCT, and their full combinations. All examples below are from the validated aircraft digital twin dataset.

[cols="2,1,2"]
|===
|Pattern |Status |Example

|GROUP BY (projected key)
|PASS
|`SELECT severity, COUNT(*) AS cnt FROM MaintenanceEvent GROUP BY severity`

|GROUP BY (non-projected key)
|PASS
|`SELECT COUNT(*) AS cnt FROM MaintenanceEvent GROUP BY severity`

|GROUP BY multi-aggregate
|PASS
|`SELECT operator, COUNT(*) AS flights, COUNT(DISTINCT origin) AS origins FROM Flight GROUP BY operator`

|HAVING (simple)
|PASS
|`SELECT operator, COUNT(*) AS cnt FROM Flight GROUP BY operator HAVING cnt > 20`

|HAVING (non-projected aggregate)
|PASS
|`SELECT severity FROM MaintenanceEvent GROUP BY severity HAVING COUNT(*) > 10`

|HAVING (compound)
|PASS
|`SELECT operator, COUNT(*) AS cnt FROM Flight GROUP BY operator HAVING COUNT(*) > 10 AND COUNT(DISTINCT origin) > 2`

|HAVING without GROUP BY
|PASS
|`SELECT COUNT(*) AS cnt FROM MaintenanceEvent HAVING COUNT(*) > 0`

|ORDER BY on aggregate alias
|PASS
|`SELECT severity, COUNT(*) AS cnt FROM MaintenanceEvent GROUP BY severity ORDER BY cnt DESC`

|ORDER BY multi-key
|PASS
|`SELECT operator, COUNT(*) AS cnt, COUNT(DISTINCT origin) AS routes FROM Flight GROUP BY operator ORDER BY cnt DESC, routes`

|DISTINCT + GROUP BY
|PASS
|`SELECT DISTINCT operator, COUNT(*) AS cnt FROM Flight GROUP BY operator`

|LIMIT + OFFSET
|PASS
|`SELECT operator, COUNT(*) AS cnt FROM Flight GROUP BY operator ORDER BY cnt DESC LIMIT 3 OFFSET 1`

|WHERE + GROUP BY
|PASS
|`SELECT COUNT(*) AS cnt FROM MaintenanceEvent WHERE severity = 'CRITICAL' GROUP BY aircraftId`

|JOIN + GROUP BY (non-projected)
|PASS
|`SELECT COUNT(*) AS flight_count FROM Flight f NATURAL JOIN DEPARTS_FROM r NATURAL JOIN Airport a GROUP BY a.code`

|JOIN + GROUP BY (projected)
|PASS
|`SELECT a.code AS code, COUNT(*) AS flight_count FROM Flight f NATURAL JOIN DEPARTS_FROM r NATURAL JOIN Airport a GROUP BY a.code`

|COUNT(DISTINCT) in HAVING
|PASS
|`SELECT operator FROM Flight GROUP BY operator HAVING COUNT(DISTINCT origin) > 3`

|Additional aggregate functions
|PASS
|`stDev(minutes)`, `stDevP(minutes)`, `percentileCont(minutes, 0.5)`, `percentileDisc(minutes, 0.5)` on `Delay`

|Full clause combination
|PASS
|`SELECT severity, COUNT(*) AS cnt FROM MaintenanceEvent GROUP BY severity HAVING COUNT(*) > 5 ORDER BY cnt DESC LIMIT 10 OFFSET 0`
|===

NOTE: All aggregation support applies to node properties only. Aggregating over relationship properties requires Cypher directly via the Neo4j Spark Connector.

=== Unsupported Patterns

[cols="1,2"]
|===
|Pattern |Workaround

|Non-aggregate `SELECT`
|Use the Neo4j Spark Connector with a Cypher query

|Relationship property aggregation
|Use Cypher directly via the Neo4j Spark Connector
|===

The practical implication: Neo4j Federated JDBC UC Connection now works well for aggregate analytics with GROUP BY, HAVING, ORDER BY, LIMIT, and DISTINCT, in addition to simple aggregates with WHERE filters and JOINs. For row-level data access or relationship property aggregation, use the Neo4j Spark Connector instead.

=== Workarounds for Unsupported Patterns

For non-aggregate queries or relationship property aggregation, the Neo4j Spark Connector bypasses UC entirely and speaks Cypher natively:

[source,python]
----
df = spark.read.format("org.neo4j.spark.DataSource") \
    .option("url", "neo4j+s://your-host") \
    .option("authentication.type", "basic") \
    .option("authentication.basic.username", user) \
    .option("authentication.basic.password", password) \
    .option("query", "MATCH (a:Aircraft) RETURN a.aircraft_id, a.manufacturer LIMIT 10") \
    .load()
----

== Validated Test Results

All patterns were validated end-to-end on Databricks Runtime 17.3 LTS against a live Neo4j Aura instance using the aircraft digital twin dataset (20 aircraft, 800 flights, 12 airports, 300 maintenance events, 514 delays). For the full script-by-script breakdown see xref:sql-reference.adoc[SQL Reference].

[cols="2,1,2"]
|===
|Test |Status |Notes

|run_01: Basic query, COUNT aggregate, Delta table access
|PASS
|20 aircraft, 172,800 sensor readings

|run_02: Three federated queries (sensor health, maintenance severity, flight delays)
|PASS
|Federated Neo4j topology + Delta aggregates

|run_03: remote_query() patterns (aggregate, WHERE, NATURAL JOIN, federated CTE)
|PASS
|Two-hop traversal: Flight → Airport

|run_04: Materialize 9 Neo4j labels as Delta, SQL validation
|PASS
|GROUP BY, WHERE + ORDER BY, aggregation + JOIN, DISTINCT

|run_05: GROUP BY (projected, non-projected, multi-aggregate)
|PASS
|All three variants

|run_05: HAVING (simple, non-projected, compound, without GROUP BY)
|PASS
|All four variants

|run_05: ORDER BY, DISTINCT, LIMIT/OFFSET
|PASS
|Including aggregate alias resolution

|run_05: WHERE + GROUP BY, JOIN + GROUP BY
|PASS
|Both projected and non-projected key variants

|run_05: COUNT(DISTINCT) in HAVING, additional aggregates
|PASS
|stDev, stDevP, percentileCont, percentileDisc

|run_05: All clauses combined + federated variants
|PASS
|GROUP BY + HAVING + ORDER BY + LIMIT + OFFSET; remote_query CTEs with Delta joins

|Non-aggregate SELECT
|EXPECTED FAIL
|Translator limitation — the SQL-to-Cypher translator does not support row-level SELECT without an aggregate. Use the Neo4j Spark Connector for row-level data access.
|===

== Choosing the Right Method

Both federation methods have a place. The right choice depends on the query pattern:

[cols="2,2"]
|===
|Use Case |Recommended Method

|Aggregate analytics (COUNT, SUM, AVG, MIN, MAX)
|Neo4j Federated JDBC UC Connection via `remote_query()` or DataFrame API

|GROUP BY / HAVING analytics
|Neo4j Federated JDBC UC Connection via `remote_query()` or DataFrame API

|Graph traversal counts (flights per airport, maintenance per system)
|Neo4j Federated JDBC UC Connection with NATURAL JOIN

|Row-level data access (list all aircraft, all flights)
|Neo4j Spark Connector

|Complex Cypher (variable-length paths, graph algorithms)
|Neo4j Spark Connector

|Relationship property aggregation
|Neo4j Spark Connector

|Ad-hoc graph exploration
|Neo4j Python Driver
|===

The federated queries in this guide use Neo4j Federated JDBC UC Connection for aggregate counts (including GROUP BY, HAVING, ORDER BY) and the Spark Connector for row-level data, combining both in a single analysis when needed. Query 5 (Fleet Health Dashboard) demonstrates this hybrid approach.

== References

* https://github.com/neo4j-partners/neo4j-uc-integration/blob/main/getting-started/01-simple-connect-test.ipynb[01-simple-connect-test.ipynb] -- runnable notebook that creates the UC JDBC connection and validates connectivity
* https://github.com/neo4j-partners/neo4j-uc-integration/blob/main/neo4j_uc_jdbc_guide.md[Neo4j Federated JDBC UC Connection Setup Guide] - Full configuration reference
* https://neo4j.com/docs/jdbc-manual/current/[Neo4j JDBC Driver Documentation]
* https://neo4j.com/docs/jdbc-manual/current/sql2cypher/[Neo4j SQL2Cypher Translation Rules]
* https://docs.databricks.com/query-federation/[Databricks Lakehouse Federation]
* https://docs.databricks.com/sql/language-manual/functions/remote_query[Databricks remote_query() Reference]
